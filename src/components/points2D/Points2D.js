// Verifound.js

import React from "react";
import "./Points2D.css"; // Importa los estilos
//import APIconfig from '../../config';  // Importamos la configuración

//const API_URL = APIconfig.API_URL_FOUNDATION1D;

const Points2D = () => {
  return (
    <div className="Points2D-container">
      <h2>Herramienta de Dibujo 2D</h2>
    </div>
  );
};

export default Points2D;
