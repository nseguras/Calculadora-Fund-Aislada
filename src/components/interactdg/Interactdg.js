// Verifound.js

import React from "react";
import "./Interactdg.css"; // Importa los estilos
//import APIconfig from '../../config';  // Importamos la configuración

//const API_URL = APIconfig.API_URL_INTERACTDG;

const Interactdg = () => {
  return (
    <div className="Interactdg-container">
      <h2>Diagrama de Interacción</h2>
    </div>
  );
};

export default Interactdg;
