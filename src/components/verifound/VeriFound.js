// Verifound.js

import React from 'react';
import './VeriFound.css'; // Importa los estilos
//import APIconfig from '../../config';  // Importamos la configuración

//const API_URL = APIconfig.API_URL_VERIFOUND; 

const VeriFound = () => {
  return (
    <div className="verifound-container">
      <h2>Verificación de Fundaciones 1D</h2>
    </div>
  );
};

export default VeriFound;
